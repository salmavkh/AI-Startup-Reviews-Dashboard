[server]
# Local development defaults.
# Hugging Face deployment uses Docker CMD flags that override these values.
address = "localhost"
port = 8501
headless = false
maxUploadSize = 300
maxMessageSize = 300
